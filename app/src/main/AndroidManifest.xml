<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.flexinfitness">
    <!-- facebook shit again -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- getting an image from camera -->
    <uses-feature android:name="android.hardware.camera" android:required="true" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.CAMERA"/>

    <application
        android:name=".BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".facebook"></activity>
        <!-- facebook shit -->
        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />

        <activity android:name=".log_proof" />
        <activity android:name=".tutorial" />
        <activity
            android:name=".workoutPlanner"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".settingOptions" />
        <activity
            android:name=".createLogEntry"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".setNotifications" />
        <activity android:name=".log" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DashBoard" /> <!-- facebook shit -->
        <provider
            android:name="com.facebook.FacebookContentProvider"
            android:authorities="com.facebook.app.FacebookContentProvider216751262760250"
            android:exported="true" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>