<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.flexinfitness">

    <application
        android:name=".BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity_tutorial"></activity>
        <activity android:name=".tutorial" />
        <activity
            android:name=".workoutPlanner"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".settingOptions" />
        <activity
            android:name=".createLogEntry"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".setNotifications" />
        <activity android:name=".logHomepage" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DashBoard" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>